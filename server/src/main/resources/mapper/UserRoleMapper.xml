<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.net.yunlou.bole.mapper.UserRoleMapper">

    <select id="selectListLeft" resultType="cn.net.yunlou.bole.model.entity.User">
        select l.*
        from t_user as l
                 left join t_user_role as et on et.user_id = l.id
        where et.role_id = #{et.roleId}
    </select>

    <select id="selectListRight" resultType="cn.net.yunlou.bole.model.entity.Role">
        select r.*
        from t_role as r
                 left join t_user_role as et on et.role_id = r.id
        where et.user_id = #{et.userId}
    </select>


</mapper>